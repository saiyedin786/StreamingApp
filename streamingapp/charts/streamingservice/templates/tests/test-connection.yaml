apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "streamingservice.fullname" . }}-test-connection"
  labels:
    {{- include "streamingservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "streamingservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
