apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chatservice.fullname" . }}-test-connection"
  labels:
    {{- include "chatservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chatservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
