apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongoservice.fullname" . }}-test-connection"
  labels:
    {{- include "mongoservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongoservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
